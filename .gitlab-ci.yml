image: node:22-alpine

stages:
  - verify

cache:
  paths:
    - node_modules/

build:
  stage: verify
  script:
    - npm install
    - npm run test
    - npm run build
    - npm run build:server
  rules:
    - if: '$CI_COMMIT_BRANCH'
