# Industry Ecosystem v1 CI gate

The feature branch must pass the repository's existing CI build and test pipeline before an MR is merged. This workstream does not modify the legacy MVP repositories.
