# Public Offline Delivery Implementation

Public offline manifests and QR resolution are exposed through the read-only `/api/v1/public` boundary. Published-content filtering, province scoping, security headers, and rate limiting remain enforced.
