# Phase 7 validation run

The branch is ready for the standard GitLab verification pipeline (`npm install`, `npm run test`, `npm run build`, `npm run build:server`).

The branch pipeline is the CI source of truth because this repository's MR pipeline rules may reject an empty MR pipeline configuration.
