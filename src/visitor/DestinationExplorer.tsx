import { useEffect, useState } from 'react';
import { ArrowUpRight, Image as ImageIcon, MapPin, RefreshCw, ShieldCheck } from 'lucide-react';
import { destinations as fallback } from '../data';

type Destination={id:string;slug:string;name:string;provinceCode:string;description?:string;contentVersion:number;updatedAt:string;freshness:'fresh'|'stale';media:Array<{id:string;publicUrl?:string;altText:string;caption?:string}>;qrPath:string;offlineCacheKey:string};
export default function DestinationExplorer(){const[data,setData]=useState<Destination[]>([]);const[loading,setLoading]=useState(true);const[offline,setOffline]=useState(false);useEffect(()=>{let alive=true;fetch('/api/v1/public/destinations').then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{if(alive)setData(x.data||[])}).catch(()=>{if(alive){setOffline(true);setData(fallback.filter(d=>d.status==='Published').map(d=>({id:d.id,slug:d.name.toLowerCase().replace(/[^a-z0-9]+/g,'-'),name:d.name,provinceCode:d.province,description:d.description,contentVersion:1,updatedAt:new Date().toISOString(),freshness:'fresh',media:[],qrPath:`/destination/${d.name}`,offlineCacheKey:`destination:${d.id}:v1`})))}).finally(()=>alive&&setLoading(false));return()=>{alive=false}},[]);return <section className="rounded-[2rem] border border-white/10 bg-[#071b2a] p-5 text-white shadow-2xl sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="text-xs font-bold uppercase tracking-[.18em] text-[#8fd5c8]">Explore PNG</div><h2 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">Destinations, published and trusted.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">Visitor content comes from the governed public projection. Versions and freshness travel with the content for safe offline and QR experiences.</p></div><button onClick={()=>window.location.reload()} className="inline-flex items-center gap-2 rounded-xl border border-white/15 px-4 py-2 text-sm font-bold hover:bg-white/10"><RefreshCw size={16}/> Refresh</button></div>{offline&&<div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/10 p-3 text-sm text-amber-100">Live projection unavailable. Showing cached visitor-safe destination cards.</div>}{loading?<div className="mt-8 text-sm text-slate-300">Loading published destinations…</div>:data.length===0?<div className="mt-8 rounded-2xl bg-white/5 p-8 text-center text-slate-300">No published destinations are available yet.</div>:<div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{data.map(d=><article key={d.id} className="overflow-hidden rounded-2xl border border-white/10 bg-white/[.06]"><div className="aspect-[16/9] bg-gradient-to-br from-[#0b6477] to-[#071b2a]">{d.media[0]?.publicUrl?<img src={d.media[0].publicUrl} alt={d.media[0].altText} className="h-full w-full object-cover"/>:<div className="flex h-full items-center justify-center text-white/30"><ImageIcon size={40}/></div>}</div><div className="p-5"><div className="flex items-center justify-between gap-3"><span className="inline-flex items-center gap-1 text-xs font-bold text-[#8fd5c8]"><MapPin size={13}/>{d.provinceCode}</span><span className="text-[11px] font-semibold text-slate-400">v{d.contentVersion} · {d.freshness}</span></div><h3 className="mt-3 text-xl font-black">{d.name}</h3><p className="mt-2 line-clamp-3 text-sm leading-6 text-slate-300">{d.description||'Discover a published destination from Papua New Guinea.'}</p><div className="mt-5 flex items-center justify-between"><span className="inline-flex items-center gap-1.5 text-xs font-semibold text-slate-400"><ShieldCheck size={14} className="text-[#f4b942]"/> Governed source</span><a href={d.qrPath} className="inline-flex items-center gap-1 text-sm font-bold text-white hover:text-[#8fd5c8]">Open <ArrowUpRight size={15}/></a></div></div></article>)}</div>}</section>}
