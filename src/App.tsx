import { useEffect, useState } from 'react';
import { BarChart3, Compass, FileText, LayoutDashboard, Map, Menu, Megaphone, Settings, ShieldCheck, Users, X, RefreshCw } from 'lucide-react';
import IndustryDiscovery from './visitor/IndustryDiscovery';
import OperatorRegistration from './visitor/OperatorRegistration';
import CommandCentre from './command-centre/CommandCentre';
import OperatorReviewQueue from './command-centre/OperatorReviewQueue';
import ActivationHub from './activation/ActivationHub';
import { ContentStudio } from './ContentStudioPage';
import VisitorHome from './VisitorHome';

type View='overview'|'visitor'|'industry'|'destinations'|'activations'|'insights';
const nav:{id:View;label:string;icon:typeof LayoutDashboard}[]=[{id:'overview',label:'Command Centre',icon:LayoutDashboard},{id:'visitor',label:'Visitor Experience',icon:Map},{id:'industry',label:'Industry Ecosystem',icon:Users},{id:'destinations',label:'Destinations & Content',icon:Compass},{id:'activations',label:'Campaigns & Events',icon:Megaphone},{id:'insights',label:'Tourism Intelligence',icon:BarChart3}];
type PublicOperator={id:string;tradingName?:string;provinceCode:string};
function App(){const[view,setView]=useState<View>('visitor');const[open,setOpen]=useState(false);const go=(next:View)=>{setView(next);setOpen(false);window.scrollTo({top:0,behavior:'smooth'})};if(view==='visitor')return <VisitorHome onAdmin={()=>go('overview')}/>;return <div className="png-shell min-h-screen"><aside className={`png-sidebar fixed inset-y-0 left-0 z-30 w-72 border-r p-5 text-white transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between"><button onClick={()=>go('visitor')} className="flex items-center gap-3 text-left"><span className="png-brand-mark flex h-10 w-10 items-center justify-center rounded-xl font-black">PNG</span><span><span className="block text-xs font-bold uppercase tracking-[.18em] text-[#8fd5c8]">Tourism Authority</span><span className="mt-0.5 block text-lg font-black">Tourism Platform</span></span></button><button className="lg:hidden" onClick={()=>setOpen(false)} aria-label="Close navigation"><X/></button></div><div className="mt-10 space-y-1">{nav.map(item=>{const Icon=item.icon;return <button key={item.id} onClick={()=>go(item.id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition ${view===item.id?'png-nav-active':'text-slate-300 hover:bg-white/5 hover:text-white'}`}><Icon size={18}/>{item.label}</button>})}</div><div className="absolute bottom-5 left-5 right-5 png-status rounded-2xl p-4"><div className="flex items-center gap-2 text-sm font-bold"><ShieldCheck size={16} className="text-[#f4b942]"/> Platform status</div><div className="mt-2 text-xs text-slate-300">Governed services online</div><div className="mt-3 flex items-center gap-2 text-[11px] font-semibold text-[#8fd5c8]"><span className="h-2 w-2 rounded-full bg-emerald-400"/> All core modules connected</div></div></aside><main className="lg:pl-72"><header className="png-header sticky top-0 z-20 border-b px-5 py-4 backdrop-blur lg:px-8"><div className="flex items-center justify-between"><button className="lg:hidden text-white" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button><div><div className="png-kicker text-xs font-bold uppercase tracking-[.16em]">Papua New Guinea Tourism Authority</div><h1 className="mt-1 text-xl font-black tracking-tight">{nav.find(n=>n.id===view)?.label}</h1></div><div className="hidden items-center gap-2 text-sm font-semibold text-slate-300 sm:flex"><Settings size={16}/> Platform Admin</div></div></header><section className="png-content px-4 py-5 sm:px-6 lg:p-8">{view==='overview'&&<div className="space-y-8"><CommandCentre/><OperatorReviewQueue/></div>}{view==='industry'&&<div className="space-y-6"><PageIntro title="Industry ecosystem" text="Connect visitors with trusted tourism businesses, experiences and enquiry journeys."/><IndustryDiscovery/><OperatorRegistration/><IndustryAdmin/></div>}{view==='destinations'&&<ContentStudio/>}{view==='activations'&&<ActivationHub/>}{view==='insights'&&<Insights/>}</section></main></div>}
function IndustryAdmin(){const[operators,setOperators]=useState<PublicOperator[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=async()=>{setLoading(true);setError('');try{const response=await fetch('/api/v1/public/operators',{headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Unable to load published operator records');const body=await response.json();const items=Array.isArray(body?.data?.items)?body.data.items:Array.isArray(body?.data)?body.data:[];setOperators(items);}catch(e){setError(e instanceof Error?e.message:'Unable to load published operator records');}finally{setLoading(false);}};useEffect(()=>{void load();},[]);return <div className="space-y-6"><PageIntro title="Industry administration" text="Published operator records are shown here for visitor-facing verification. Regulatory registration, approval and compliance mutations remain protected behind authenticated authority roles."/><div className="rounded-2xl border border-slate-200/80 bg-white/90 p-5 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-xs font-bold uppercase tracking-[.16em] text-emerald-700">Published operator registry</div><h3 className="mt-1 text-xl font-bold">Compliant tourism operators</h3></div><button onClick={()=>void load()} className="inline-flex items-center gap-2 rounded-xl border border-slate-300 px-3 py-2 text-sm font-semibold"><RefreshCw size={15}/> Refresh</button></div>{error&&<div role="status" className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">{error}</div>}{loading?<div className="mt-5 rounded-xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">Loading published operators…</div>:operators.length===0?<div className="mt-5 rounded-xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center"><div className="font-semibold text-slate-700">No compliant operators are published yet.</div><p className="mt-2 text-sm text-slate-500">This is an intentional empty state. Operators must complete the governed registration, approval and compliance process before appearing in visitor discovery.</p></div>:<div className="mt-5 overflow-hidden rounded-xl border border-slate-200"><table className="w-full text-left text-sm"><thead className="bg-slate-100 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Operator</th><th className="p-4">Province</th><th className="p-4">Public status</th></tr></thead><tbody>{operators.map(operator=><tr key={operator.id} className="border-t border-slate-100"><td className="p-4 font-semibold">{operator.tradingName||'Tourism operator'}</td><td className="p-4 text-slate-600">{operator.provinceCode}</td><td className="p-4"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">Published · compliant</span></td></tr>)}</tbody></table></div>}</div></div>}
function Insights(){return <div className="space-y-6"><PageIntro title="Tourism intelligence" text="The intelligence layer consolidates visitor, industry, destination and governed operational signals into actionable reporting."/><div className="grid gap-5 md:grid-cols-3"><Insight icon={BarChart3} title="Executive intelligence" text="National performance, trends and strategic indicators."/><Insight icon={Map} title="Provincial intelligence" text="Destination activity, operator health and local opportunity signals."/><Insight icon={FileText} title="Regulatory reporting" text="Licensing, compliance, registrations and audit evidence."/></div></div>}
function PageIntro({title,text}:{title:string;text:string}){return <div><h2 className="png-section-title text-3xl font-black tracking-tight">{title}</h2><p className="mt-2 max-w-3xl text-slate-600">{text}</p></div>}
function Insight({icon:Icon,title,text}:{icon:typeof BarChart3;title:string;text:string}){return <div className="rounded-3xl border border-slate-200/80 bg-white/90 p-6 shadow-sm"><Icon className="text-[#0b6477]"/><h3 className="mt-4 font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div>}
export default App;
